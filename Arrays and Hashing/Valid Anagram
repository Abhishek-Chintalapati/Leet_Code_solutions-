My Solution 

class Solution:
   def isAnagram(self, s: str, t: str) -> bool:
       first_list = list(s)
       second_list = list(t)
       sorted_first = str(sorted(first_list))
       sorted_second = str(sorted(second_list))
       if sorted_first == sorted_second:
           return True
       else:
           return False


Efficient Solution 

class Solution:
   def isAnagram(self, s: str, t: str) -> bool:
       if len(s) != len(t):
           return False
       dict_store = {}
       for i in s:
           dict_store[i] = dict_store.get(i,0) + 1
      
       for j in t:
           if j in dict_store and dict_store[j] > 0:
               dict_store[j] = dict_store[j] - 1
           else:
               return False
       return True
