My solution which got time limit exceeded error 

class Solution:
    def maxArea(self, height: List[int]) -> int:
        final_area = float('-inf')
        for i in range(len(height)-1):
            l,r = i, len(height)-1
            while l < r:
                length = min(height[l],height[r])
                width = r - l
                area = length * width
                final_area = max(area, final_area)
                r -= 1
        return final_area


Best Solution: 

class Solution:
    def maxArea(self, height: List[int]) -> int:
        final_area = 0
        l,r = 0, len(height)-1
        while l < r:
            length = min(height[l],height[r])
            width = r - l
            area = length * width
            final_area = max(area, final_area)
            if height[l] < height[r]:
                l += 1
            else:
                r -= 1
        return final_area
        
